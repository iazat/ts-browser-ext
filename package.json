{
  "name": "ts-browser-ext",
  "private": true,
  "description": "Tests for the Chrome and Firefox extension front-ends",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:background": "node --test tests/background.test.mjs",
    "test:popup": "node --test tests/popup.test.mjs",
    "icons": "node script/icons.mjs",
    "screenshots": "node script/screenshots.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
